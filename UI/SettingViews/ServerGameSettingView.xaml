<UserControl x:Class="GameTech.Elite.Client.Modules.B3Center.UI.SettingViews.ServerGameSettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm ="clr-namespace:GameTech.Elite.Client.Modules.B3Center.ViewModels.Settings"
             mc:Ignorable="d" 
             >
    <Border>
        <Grid    HorizontalAlignment="Center"  VerticalAlignment="Top" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="279"/>
                <ColumnDefinition Width="75"/>
                <ColumnDefinition Width="279"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel  Grid.Column="1" Grid.Row="0">
                <Label  Content="Min Player"/>
                <TextBox Text="{Binding ServerSetting_.MinPlayer}"  MaxLength="9" />
                <Label  Content="Game Start Delay(sec)"/>
                <TextBox Text="{Binding ServerSetting_.GameStartDelay}"     MaxLength="9"   />
                <Label  Content="Consolation Prize"/>
                <TextBox Text="{Binding ServerSetting_.Consolation}"    MaxLength="9"  />
                <Label Content="Game Recall Password"/>
                <TextBox Text="{Binding ServerSetting_.GameRecallPassw}"   MaxLength="25"/>
                <Label   Content="Wait Count Down(sec)"/>
                <TextBox Text="{Binding ServerSetting_.WaitCountDown}"  MaxLength="25"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>


<!--Tag="38"--> 