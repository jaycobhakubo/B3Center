<UserControl x:Class="GameTech.Elite.Client.Modules.B3Center.UI.SettingViews.SalesSettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
                  d:DesignHeight="710" d:DesignWidth="824">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="VisibilityConverter" />
        <!--<Style TargetType="Button" BasedOn="{StaticResource FlatWhiteMediumButtonStyle}"/>
        <Style TargetType="CheckBox" BasedOn="{StaticResource DefaultDesignForCheckBox}"/>-->

        <Style  TargetType="TextBox">
            <Setter Property="Background" Value="White" />
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Light"/>
            <Setter Property="FontSize" Value="15" />
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="BorderBrush" Value="LightGray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Text" Value="00.00"/>
            <Setter Property="Height" Value="31"/>
            <Setter Property="Padding" Value="8,0,0,0"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border SnapsToDevicePixels="True"  BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}" Background="{TemplateBinding Background}" >
                            <ScrollViewer 
                                x:Name="PART_ContentHost" 
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" 
                                VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Hidden" />
                        </Border>
                        <ControlTemplate.Triggers>

                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="Blue"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="False">
                                <Setter Property="BorderBrush" Value="LightGray"/>
                            </Trigger>

                            <Trigger Property="IsReadOnly" Value="True">
                                <Setter Property="Background" Value="LightGray"></Setter>
                            </Trigger>

                        </ControlTemplate.Triggers>


                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type Label}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontWeight" Value="Light"/>
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Width" Value="Auto"/>
            <Setter Property="Height" Value="18"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Padding" Value="0 5 0 2"/>
            <Setter Property="Margin" Value="0 10 0 0"/>
        </Style>
    </UserControl.Resources>
    <Border>
        <Grid Name="gridSalesSettings" GotFocus="Grid_GotFocus">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="75"/>
                <ColumnDefinition Width="279"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="279"/>
                <ColumnDefinition Width="75"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel Name="stkpnlSalesSetting1" Grid.Row="1" Grid.Column="1">
              
                <Label x:Name="lblCallSpeedMin_Copy"  Content="Log Recycle Days"  VerticalAlignment="Top"   Height="Auto" Margin="0" Padding="0,0,0,2"/>
                <TextBox PreviewTextInput="txtbxNumericOnly_PreviewTextInput" Name="txtbxLogRecycleDays"  TextWrapping="Wrap" VerticalAlignment="Bottom" Text=""  TabIndex="10" Tag="32" MaxLength="9" PreviewKeyDown="txtbxLogRecycleDays_PreviewKeyDown" />               
                <Label x:Name="lblCallSpeedMin" Content="Main Vol" HorizontalAlignment="Left"  Height="Auto"  VerticalAlignment="Top"/>
                <ComboBox 
            	x:Name="cmbxMainVolume" 
            	Grid.Row="1" 
            	Grid.Column="3" 
            	 VerticalAlignment="Bottom"  TabIndex="9" Height="22" 
            	FontWeight="Light" FontFamily="Segoe UI" Tag="33">
                </ComboBox>
                <CheckBox Name="chkbxScreenCursor" Content="Screen Cursor"  HorizontalAlignment="Left"   TabIndex="1" Tag="24"  Margin="0 31 0 0"/>
                <CheckBox Name="chkbxAutoPrintSession" Content="Auto Print Session Report"   HorizontalAlignment="Left"  Margin="0 30 0 0" TabIndex="2" Tag="26" />
                <CheckBox Name="chkbxAllowSessionBallChange" Content="Allow in Session Ball Change" Margin="0 30 0 0" HorizontalAlignment="Left"   Width="232" TabIndex="4" Tag="30" />
                <CheckBox Name="chkbxPrintLogo" Content="Print Logo"  HorizontalAlignment="Left" Margin="0 30 0 0"  TabIndex="5" Tag="29" />
                <CheckBox Name="chkbxAutoPagePrinter" Content="Page Printer" Margin="0 30 0 0" HorizontalAlignment="Left"    TabIndex="6" Tag="27" />
                <CheckBox Name="chkbxCalibrateTouch" Content="Calibrate Touch" Margin="0 30 0 0" HorizontalAlignment="Left"   TabIndex="7" Tag="25" />
                <CheckBox Name="chkbxLoggingEnable" Content="Logging Enable" Margin="0 30 0 0" HorizontalAlignment="Left"   TabIndex="8" Tag="31"  />
             
            </StackPanel>
            
            <StackPanel Name="stkpnlSalesSetting2" Grid.Column="3" Grid.Row="1">
                <CheckBox Name="chkbxQuickSales" Content="Quick Sales" Margin="0 31 0 0" HorizontalAlignment="Left"  TabIndex="3" Tag="28" />
            </StackPanel>
          
            <!--<Grid Grid.Row="18" Grid.ColumnSpan="6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="2"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="5"></RowDefinition>
                </Grid.RowDefinitions>

                <Border
                        Grid.Row="0"
                    	Height="43" 
                    	Width="360"
                    	HorizontalAlignment ="Right"
                        VerticalAlignment="Bottom"
                        >
                    <ContentPresenter 
                    		x:Name="SaveCancelTransition" 
                    		Height="43" 
                    		VerticalAlignment="Bottom" 
                    		HorizontalAlignment="Left" Visibility="Hidden"/>
                </Border>-->

            <!--</Grid>-->

            <!--<Grid Grid.Column="3" Grid.ColumnSpan="3">
                <Label x:Name="lblSavedNotification"
                            Background="#44658D"
                               Foreground="White"
                               FontStyle="Italic"
                               FontWeight="ExtraLight"
                                Grid.Column="5"
                                Content="Saved Successfully"
                                Padding="5,2,5,3"
                                VerticalContentAlignment="Center"
                                VerticalAlignment="Top" 
                                HorizontalAlignment="Right"
                               Style="{x:Null}"
                               >

                </Label>
            </Grid>-->
            <!--<Label 
                x:Name="lblNewOperator" 
                Content="Sales" 
                Grid.Column="1"
                HorizontalAlignment="Left"  
                Grid.Row="0" 
                VerticalAlignment="Center"
                FontSize="26" 
                Height="Auto" 
                Width="163" 
                Margin="0,7.5"/>-->


        </Grid>
    </Border>
</UserControl>
