<UserControl x:Class="GameTech.Elite.Client.Modules.B3Center.UI.SettingViews.PlayerSettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
            d:DesignHeight="710" d:DesignWidth="824">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="VisibilityConverter" />
        <!--<Style TargetType="Button" BasedOn="{StaticResource FlatWhiteMediumButtonStyle}"/>
        <Style TargetType="CheckBox" BasedOn="{StaticResource DefaultDesignForCheckBox}"/>-->

        <Style  TargetType="TextBox">
            <Setter Property="Background" Value="White" />
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Light"/>
            <Setter Property="FontSize" Value="15" />
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="BorderBrush" Value="LightGray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Text" Value="00.00"/>
            <Setter Property="Height" Value="31"/>
            <Setter Property="Padding" Value="8,0,0,0"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border SnapsToDevicePixels="True"  BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}" Background="{TemplateBinding Background}" >
                            <ScrollViewer 
                                x:Name="PART_ContentHost" 
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" 
                                VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Hidden" />
                        </Border>
                        <ControlTemplate.Triggers>

                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="Blue"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="False">
                                <Setter Property="BorderBrush" Value="LightGray"/>
                            </Trigger>

                            <Trigger Property="IsReadOnly" Value="True">
                                <Setter Property="Background" Value="LightGray"></Setter>
                            </Trigger>

                        </ControlTemplate.Triggers>


                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type Label}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontWeight" Value="Light"/>
            <Setter Property="SnapsToDevicePixels" Value="True" />

            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Width" Value="Auto"/>
            <Setter Property="Height" Value="18"/>
            <Setter Property="Padding" Value="0 5 0 2"/>
            <Setter Property="Margin" Value="0 10 0 0"/>
        </Style>
    </UserControl.Resources>
    <Border>
        <Grid Name="gridPlayerSetting" GotFocus="Grid_GotFocus" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="75"/>
                <ColumnDefinition Width="279"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="279"/>
                <ColumnDefinition Width="75"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <StackPanel Name="stkpnlPlayerSetting" Grid.Column="1" Grid.Row="1">
                <Label x:Name="lblCallSpeedMin_Copy2"  Content="Time To Collect (milliseconds)"  Height="Auto" Padding="0 0 0 2" Margin="0" />
                <TextBox PreviewTextInput="txtbxNumericOnly_PreviewTextInput" Name="txtbxTimeToCollect" TextWrapping="Wrap" VerticalAlignment="Bottom" Text="" TabIndex="7" Tag="20" MaxLength="9" PreviewKeyDown="txtbx_PreviewKeyDown" />
                
                <Label x:Name="lblCallSpeedMin" Grid.Row="1" Grid.Column="3" Content="Main Vol" HorizontalAlignment="Left"    Width="66" Height="Auto"  VerticalAlignment="Stretch"/>
                <ComboBox 
            	x:Name="cmbxMainVol" 
            	Grid.Row="1" 
            	Grid.Column="3" 
            	HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Width="Auto" TabIndex="8" Height="22" 
            	FontWeight="Light" FontFamily="Segoe UI" Tag="23">
                </ComboBox>

                <CheckBox Name="chkbxScreenCursor"  Content="Screen Cursor" Grid.Column="1" HorizontalAlignment="Left"  Grid.Row="7" TabIndex="4" Tag="19"  Margin="0 31 0 0"/>
                <CheckBox Name="chkbxCalibrateTouch"  Content="Calibrate Touch"  HorizontalAlignment="Left"  Tag="16" Margin="0 30 0 0" />
                <CheckBox Name="chkbxAnnounceCall" Content="Announce Call" Grid.Column="1" HorizontalAlignment="Left"  Grid.Row="5" TabIndex="3" Tag="18" Margin="0 30 0 0"/>
                <CheckBox Name="chkbxPressToCollect"  Content="Press To Collect" Grid.Column="1" HorizontalAlignment="Left"  Grid.Row="3"  TabIndex="2" Tag="17" Margin="0 30 0 0" />
                <CheckBox Name="chkbxDisclaimer"  Content="Disclaimer" Grid.Column="1" HorizontalAlignment="Left"  Grid.Row="9"  TabIndex="5" Tag="21" Margin="0 30 0 0"/>
            </StackPanel>
            
            <StackPanel Grid.Column="3" Grid.Row="1">
              
            </StackPanel>
                          
            <!--<Grid Grid.Row="19" Grid.ColumnSpan="6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="2"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="5"></RowDefinition>
                </Grid.RowDefinitions>

                <Border
                        Grid.Row="0"
                    	Height="43" 
                    	Width="360"
                    	HorizontalAlignment ="Right"
                        VerticalAlignment="Bottom"
                        >
                    <ContentPresenter 
                    		x:Name="SaveCancelTransition" 
                    		Height="43" 
                    		VerticalAlignment="Bottom" 
                    		HorizontalAlignment="Left" Visibility="Hidden"/>
                </Border>
            </Grid>-->

            <!--<Grid Grid.Column="3" Grid.ColumnSpan="3">
                <Label x:Name="lblSavedNotification"
                            Background="#44658D"
                               Foreground="White"
                               FontStyle="Italic"
                               FontWeight="ExtraLight"
                                Grid.Column="5"
                                Content="Saved Successfully"
                                Padding="5,2,5,3"
                                VerticalContentAlignment="Center"
                                VerticalAlignment="Top" 
                                HorizontalAlignment="Right"
                               Style="{x:Null}"
                               >

                </Label>
            </Grid>-->
            <!--<Label x:Name="lblNewOperator" Content="Player" Grid.Column="1" HorizontalAlignment="Left"  Grid.Row="0" VerticalAlignment="Center" FontSize="26" Height="Auto" Width="163" Margin="0,7.5"/>-->
        </Grid>

    </Border>
</UserControl>
